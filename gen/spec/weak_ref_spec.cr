require "spec"

it "weak_ref.cr" do
puts # :nocode:

puts "# weak_ref.cr:3 (001.cr)" # :nocode: :example:
# pending by 'file:pendings/weak_ref/001.cr' # :pending:
# ref = WeakRef.new("oof".reverse)
# p ref.value # => "foo"
# GC.collect
# p ref.value # => nil


end
